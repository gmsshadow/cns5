<section class="tab {{tab.cssClass}}" data-tab="{{tab.id}}" data-group="{{tab.group}}">
    <div class="cns5-stub">
        <h2>{{localize tab.label}}</h2>
        <p>{{localize planned}}</p>
    </div>
</section>
